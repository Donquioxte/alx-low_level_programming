#include <stdio.h>
#include "main.h"
/**
 * swap - a function that swaps the values of two integers
 *
 * Return 0(Success);
 */

void swap_int(int *a, int *b)
{
	int temp = *a;
	*a = *b;
	*b = temp
	swap(*a, *b);
}
