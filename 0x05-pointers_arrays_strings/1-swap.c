#include <stdio.h>
#include "main.h"
/**
 * swap_int - a function that swaps the values of two integers
 *
 * @a: an integer input function
 * @b: an integer input function
 * Return 0(Success);
 */

void swap_int(int *a, int *b)

int main(void);
{
	int temp = *a;
	*a = *b;
	*b = temp;
	swap(*a, *b);
}
