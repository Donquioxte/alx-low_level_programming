#include<main.h>
#include <stdio.h>
/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return:0 (Success)
 */
int  main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z' ch++)
		_putchar(ch);

	_putchar('\n');
}
